import { Container, Typography } from '@mui/material';

const Cart = () => {
  return (
    <Container>
      <Typography variant="h4" component="h2" sx={{ my: 4 }}>
        Shopping Cart
      </Typography>
      {/* Cart items will be added later */}
    </Container>
  );
};

export default Cart; 