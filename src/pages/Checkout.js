import { Container, Typography } from '@mui/material';

const Checkout = () => {
  return (
    <Container>
      <Typography variant="h4" component="h2" sx={{ my: 4 }}>
        Checkout
      </Typography>
      {/* Checkout form will be added later */}
    </Container>
  );
};

export default Checkout;