import { Container, Typography } from '@mui/material';

const Products = () => {
  return (
    <Container>
      <Typography variant="h4" component="h2" sx={{ my: 4 }}>
        Our Products
      </Typography>
      {/* Product listing will be added later */}
    </Container>
  );
};

export default Products; 